import React from "react";
import { Squash as Hamburger } from "hamburger-react";

const Nav = (): JSX.Element => {
  return (
    <nav className="nav">
      <Hamburger />
    </nav>
  );
};

export default Nav;
